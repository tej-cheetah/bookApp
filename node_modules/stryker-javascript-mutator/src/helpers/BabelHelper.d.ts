import * as types from '@babel/types';
import { NodeWithParent } from './ParentNode';
export default class BabelHelper {
    static parse(code: string): types.File;
    private static createOptions;
    static getNodes(ast: types.File): NodeWithParent[];
    static generateCode(ast: types.Node): string;
}
